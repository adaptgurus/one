name: T2 Foundation Resume Install Superseded

on:
  push:
    branches:
      - ops/t2-foundation-iso-qualification-20260921
    paths:
      - .github/workflows/t2-foundation-resume-install.yml

permissions:
  contents: read

concurrency:
  group: t2-foundation-resume-install
  cancel-in-progress: true

jobs:
  superseded:
    runs-on: ubuntu-24.04
    steps:
      - name: Do not mutate qualification VM
        run: |
          echo "Superseded by T2 Foundation Anaconda Continue."
          echo "No VM or console mutation is performed by this workflow."
