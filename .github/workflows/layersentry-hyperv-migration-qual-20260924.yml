name: LayerSentry Hyper-V Migration Qualification

on:
  pull_request:
    types: [opened, synchronize, reopened]

permissions:
  contents: read

concurrency:
  group: layersentry-hyperv-migration-qual-20260924
  cancel-in-progress: true

jobs:
  inventory:
    if: github.event.pull_request.head.ref == 'qual/hyperv-migration-live-20260924'
    runs-on: [self-hosted, Windows, X64]
    timeout-minutes: 20
    steps:
      - name: Bind exact Hyper-V host
        shell: powershell
        run: |
          $ErrorActionPreference='Stop'
          Write-Host "QUAL_HOST runner=$env:RUNNER_NAME computer=$env:COMPUTERNAME user=$env:USERNAME"
          if($env:RUNNER_NAME -ne 'Manoj-Test-Inst'){throw "wrong runner: $env:RUNNER_NAME"}
          if($env:COMPUTERNAME -ine 'MANOJ-TEST-INST'){throw "wrong computer: $env:COMPUTERNAME"}
          Get-Command Get-VM,Get-VHD,Get-VMHardDiskDrive,Get-VMNetworkAdapter,Get-VMSwitch -ErrorAction Stop | Out-Null

      - name: Inventory Hyper-V VMs and disks
        shell: powershell
        run: |
          $ErrorActionPreference='Stop'
          Write-Host '===VMS==='
          Get-VM | Sort-Object Name | ForEach-Object {
            [pscustomobject]@{
              Name=$_.Name; State=$_.State; Gen=$_.Generation; CPU=$_.ProcessorCount;
              MemGB=[math]::Round($_.MemoryAssigned/1GB,1);
              AutoCheckpoints=$_.AutomaticCheckpointsEnabled; Path=$_.Path
            }
          } | Format-Table -AutoSize

          Write-Host '===DISKS==='
          $rows=@()
          foreach($vm in Get-VM){
            foreach($d in Get-VMHardDiskDrive -VM $vm -ErrorAction SilentlyContinue){
              $v=$null; try{$v=Get-VHD -Path $d.Path -ErrorAction Stop}catch{}
              $rows += [pscustomobject]@{
                VM=$vm.Name; Controller=$d.ControllerType; Location=$d.ControllerLocation;
                Path=$d.Path; Format=if($v){$v.VhdFormat}else{'?'}; Type=if($v){$v.VhdType}else{'?'};
                SizeGB=if($v){[math]::Round($v.Size/1GB,1)}else{0};
                FileGB=if($v){[math]::Round($v.FileSize/1GB,1)}else{0};
                Parent=if($v){$v.ParentPath}else{''}
              }
            }
          }
          $rows | Sort-Object VM,Location | Format-Table -AutoSize -Wrap

      - name: Inventory VM networks and switches
        shell: powershell
        run: |
          $ErrorActionPreference='Stop'
          Write-Host '===NETWORKS==='
          $netrows=@()
          foreach($vm in (Get-VM | Sort-Object Name)){
            foreach($a in (Get-VMNetworkAdapter -VM $vm -ErrorAction SilentlyContinue)){
              $netrows += [pscustomobject]@{
                VM=$vm.Name; Name=$a.Name; Switch=$a.SwitchName; MAC=$a.MacAddress;
                Status=$a.Status; IPs=($a.IPAddresses -join ',')
              }
            }
          }
          $netrows | Format-Table -AutoSize -Wrap
          Write-Host '===SWITCHES==='
          Get-VMSwitch | Select-Object Name,SwitchType,NetAdapterInterfaceDescription | Format-Table -AutoSize

      - name: Inspect migration qualification assets
        shell: powershell
        run: |
          $ErrorActionPreference='Stop'
          $root='C:\LayerSentryLab\qualification'
          if(-not(Test-Path $root)){throw "missing $root"}
          Write-Host '===QUAL_TOP==='
          Get-ChildItem -LiteralPath $root -Force |
            Select-Object Name,Length,LastWriteTime,Attributes |
            Sort-Object Name | Format-Table -AutoSize
          Write-Host '===MIGRATION_CANDIDATES==='
          foreach($name in @('ls-hv-migration-source','ls-hv-migration-source2','ls-hv-migration-source3','ls-hv-migration-source4','ls-migration-source-test')){
            $vm=Get-VM -Name $name -ErrorAction SilentlyContinue
            if(-not $vm){continue}
            Write-Host "CANDIDATE name=$name state=$($vm.State) gen=$($vm.Generation) cpu=$($vm.ProcessorCount) mem_bytes=$($vm.MemoryAssigned) checkpoints=$((Get-VMSnapshot -VMName $name -ErrorAction SilentlyContinue | Measure-Object).Count)"
            foreach($d in (Get-VMHardDiskDrive -VMName $name -ErrorAction SilentlyContinue)){
              if(-not $d.Path){continue}
              $v=Get-VHD -Path $d.Path -ErrorAction SilentlyContinue
              Write-Host "CANDIDATE_DISK vm=$name path=$($d.Path) format=$($v.VhdFormat) type=$($v.VhdType) size=$($v.Size) file=$($v.FileSize) parent=$($v.ParentPath)"
            }
            foreach($a in (Get-VMNetworkAdapter -VMName $name -ErrorAction SilentlyContinue)){
              Write-Host "CANDIDATE_NIC vm=$name switch=$($a.SwitchName) mac=$($a.MacAddress) ips=$($a.IPAddresses -join ',')"
            }
          }
          Write-Host '===MIGRATION_SOURCE_TREE==='
          $ms='C:\LayerSentryLab\migration-source'
          if(Test-Path $ms){
            Get-ChildItem -LiteralPath $ms -Recurse -Force -ErrorAction SilentlyContinue |
              Select-Object -First 200 FullName,Length,LastWriteTime |
              Format-Table -AutoSize -Wrap
          }
          Write-Host '===MIGRATION_ASSETS==='
          Get-ChildItem -LiteralPath $root -Recurse -Force -ErrorAction SilentlyContinue |
            Where-Object { $_.FullName -match '(?i)(oneswap|migration|virtio|hyperv|qcow2)' } |
            Select-Object -First 200 FullName,Length,LastWriteTime |
            Format-Table -AutoSize -Wrap
          Write-Host '===SOURCE_HOST_SSH==='
          Get-Service sshd -ErrorAction SilentlyContinue | Select-Object Name,Status,StartType | Format-Table -AutoSize
          Get-NetIPAddress -AddressFamily IPv4 -ErrorAction SilentlyContinue |
            Where-Object {$_.IPAddress -notmatch '^127\.'} |
            Select-Object InterfaceAlias,IPAddress,PrefixLength |
            Format-Table -AutoSize

      - name: Current migration path connectivity
        shell: powershell
        run: |
          $ErrorActionPreference='Continue'
          function Test-TcpFast([string]$ip,[int]$port){
            $client=New-Object System.Net.Sockets.TcpClient
            try{
              $iar=$client.BeginConnect($ip,$port,$null,$null)
              if(-not $iar.AsyncWaitHandle.WaitOne(2500)){ return $false }
              $client.EndConnect($iar)
              return $client.Connected
            }catch{return $false}finally{$client.Close()}
          }
          foreach($ip in @('10.250.10.40','10.250.10.41','10.250.10.42','10.250.10.43')){
            foreach($port in @(22,9443)){
              $ok=Test-TcpFast $ip $port
              Write-Host "CONNECT ip=$ip port=$port tcp=$ok"
            }
          }
          Get-PSDrive -PSProvider FileSystem | ForEach-Object {
            Write-Host "HOST_STORAGE drive=$($_.Name) free_gb=$([math]::Round($_.Free/1GB,1)) used_gb=$([math]::Round($_.Used/1GB,1))"
          }

      - name: Bind disposable guest seed and SSH
        shell: powershell
        run: |
          $ErrorActionPreference='Stop'
          $root='C:\LayerSentryLab\qualification\ls-migration-source-test'
          $network=Get-Content -Raw -LiteralPath (Join-Path $root 'network-config')
          $userData=Get-Content -Raw -LiteralPath (Join-Path $root 'user-data')
          $ipv4=[regex]::Matches($network,'(?<![0-9])(?:10|172|192)\.[0-9]+\.[0-9]+\.[0-9]+') |
            ForEach-Object {$_.Value} | Where-Object {$_ -ne '10.250.10.1'} | Select-Object -First 1
          $userMatch=[regex]::Match($userData,'(?m)^\s*-?\s*name:\s*([A-Za-z0-9_.-]+)\s*$')
          if(-not $ipv4){throw 'no seeded source IPv4 found'}
          if(-not $userMatch.Success){throw 'no seeded source SSH username found'}
          $user=$userMatch.Groups[1].Value
          Write-Host "SOURCE_SEED ip=$ipv4 user=$user"
          $key=Join-Path $root 'source_ci_ed25519'
          $known=Join-Path $env:RUNNER_TEMP 'source-known-hosts'
          ssh-keyscan.exe -T 5 $ipv4 2>$null | Out-File -FilePath $known -Encoding ascii
          if(-not(Test-Path $known) -or (Get-Item $known).Length -eq 0){throw "source guest SSH unavailable at $ipv4"}
          ssh.exe -o BatchMode=yes -o StrictHostKeyChecking=yes -o "UserKnownHostsFile=$known" -i $key "$user@$ipv4" "hostname; id -un; uname -a; lsblk -b -o NAME,TYPE,SIZE,MOUNTPOINTS"
          if($LASTEXITCODE -ne 0){throw "source guest SSH failed: $LASTEXITCODE"}
          Write-Host 'SOURCE_GUEST_SSH=PASS'

      - name: Bind authorized builder and exact migration source
        shell: powershell
        env:
          EXPECTED_SHA: e5a0c7e0101b47dc80e1378cbd69b7614e905ca1
        run: |
          $ErrorActionPreference='Stop'
          $key='C:\LayerSentryLab\qualification\iso-builder-key'
          $known='C:\LayerSentryLab\qualification\known_hosts_builder'
          $discover='set -e; for p in /home/builder/layersentry-platform /home/builder/*/layersentry-platform /opt/layersentry-platform /srv/layersentry-platform; do [ -d "$p/.git" ] || continue; echo "BUILDER_REPO=$p"; git -C "$p" rev-parse HEAD; done'
          $out=& ssh.exe -o BatchMode=yes -o StrictHostKeyChecking=yes -o "UserKnownHostsFile=$known" -i $key builder@10.250.10.40 $discover
          if($LASTEXITCODE -ne 0){throw "builder discovery failed: $LASTEXITCODE"}
          $out | Write-Host
          $repoLine=$out | Where-Object {$_ -like 'BUILDER_REPO=*'} | Select-Object -First 1
          if(-not $repoLine){throw 'builder has no LayerSentry source checkout'}
          $remoteRepo=$repoLine.Substring('BUILDER_REPO='.Length)
          $sha=$env:EXPECTED_SHA
          $check="set -e; repo='$remoteRepo'; sha='$sha'; if ! git -C \"$remoteRepo\" cat-file -e \"$sha^{commit}\" 2>/dev/null; then git -C \"$remoteRepo\" fetch --depth=1 origin \"$sha\"; fi; git -C \"$remoteRepo\" cat-file -e \"$sha^{commit}\"; echo EXACT_SOURCE=$sha"
          & ssh.exe -o BatchMode=yes -o StrictHostKeyChecking=yes -o "UserKnownHostsFile=$known" -i $key builder@10.250.10.40 $check
          if($LASTEXITCODE -ne 0){throw "exact source unavailable on builder: $LASTEXITCODE"}
