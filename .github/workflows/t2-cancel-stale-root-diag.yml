name: T2 Cancel active bootstrap-fix installer retry

on:
  push:
    branches:
      - ops/t2-install-4c4ac81
    paths:
      - .github/workflows/t2-cancel-stale-root-diag.yml
      - qualification/t2-cancel-stale-root-diag.request

permissions:
  actions: write
  contents: read

jobs:
  cancel:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - name: Force-cancel active bootstrap-fix installer retry
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          set -euo pipefail
          code=$(curl -sS -o /tmp/cancel.out -w '%{http_code}' -X POST \
            -H "Authorization: Bearer ${GH_TOKEN}" \
            -H 'Accept: application/vnd.github+json' \
            -H 'X-GitHub-Api-Version: 2022-11-28' \
            "https://api.github.com/repos/${GITHUB_REPOSITORY}/actions/runs/35764338005/force-cancel")
          cat /tmp/cancel.out || true
          case "$code" in 202|409) ;; *) echo "cancel HTTP $code" >&2; exit 1 ;; esac
          echo "BOOTSTRAPFIX_RETRY_CANCEL=PASS http=$code"